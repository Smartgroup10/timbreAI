[general]
enabled=yes
pretty=yes
allowed_origins=*

; Credenciales internas para el ARI WebSocket entre backend ↔ asterisk.
; El password aquí DEBE coincidir con la env ASTERISK_ARI_PASSWORD del backend.
; Asterisk no interpola env vars en .conf, así que se queda literal.
; Como ARI no está expuesto fuera de la red Docker (`expose:` interno), el
; riesgo es bajo; rótalo editando este archivo + la env del backend.
[timbre]
type=user
read_only=no
password=ast3r1sKpasSw0rD
