[general]
static=yes
writeprotect=no

[globals]

[from-internal]
exten => 1000,1,Answer()
 same => n,Playback(hello-world)
 same => n,Hangup()

exten => 9000,1,NoOp(Atrium AI test entrypoint)
 same => n,Answer()
 same => n,Stasis(atrium-call-bot)
 same => n,Hangup()

[from-trunk]
exten => _X.,1,NoOp(Inbound call from trunk)
 same => n,Answer()
 same => n,Stasis(atrium-inbound-bot)
 same => n,Hangup()

